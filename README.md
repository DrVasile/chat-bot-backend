# Chatbot Back-end powered by OpenAI API LLM


## Running in a Docker container
```
docker build -t chatbot .
docker run -p 8080:8080 chatbot
```


## Running with Docker compose
```
docker compose up
```
